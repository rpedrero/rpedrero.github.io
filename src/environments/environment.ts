export const environment = {
  localStorage: true, //True if data should be accessed and saved though a local storage (mock) rather than using a REST API.
  apiUrl: '' //If using a REST CRUD, must contain the URL of the API server.
};
